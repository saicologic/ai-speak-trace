{
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/set-version.mjs && npm run pkg:backend && tauri build && cd src-tauri/target/release/bundle/dmg && mv *_*_*.dmg 'AI Speak Trace.dmg'",
    "tauri": "tauri",
    "pkg:backend": "cd backend && npm run pkg:mac-arm64"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "concurrently": "^9.2.1"
  }
}
